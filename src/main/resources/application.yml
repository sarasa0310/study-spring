server:
  port: 8080
management:
  server:
    port: 9000
    address: 127.0.0.1
  endpoints:
    web:
      base-path: /management
      exposure:
        include: health,beans,conditions,env,mappings
